name: allowance_shared
description: Shared models, services, and theme for allowance apps
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  path_provider: ^2.1.5
  pdf: ^3.12.0
  printing: ^5.14.3
  share_plus: 12.0.0
  http: ^1.2.2

flutter:
  uses-material-design: true
  assets:
    - assets/fonts/
